// This file (config.js) is AUTO-GENERATED from the MAPBOX_TOKEN env var by
// scripts/gen-mapbox-config.mjs, which runs on `npm run dev` and `npm run build`.
//
// Production (Vercel): set MAPBOX_TOKEN in Project Settings → Environment Variables.
// Local dev: either export MAPBOX_TOKEN, or copy this file to config.js and set the token by hand.
// config.js is gitignored so the token never enters the repo.
// Use a PUBLIC (pk.) token and restrict it by URL in your Mapbox account.
window.MAPBOX_TOKEN = 'pk.your-mapbox-public-token-here';
