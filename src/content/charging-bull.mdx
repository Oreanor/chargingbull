import { OPENER_MODEL, OPENER_FRAMES, OPENER_PLACEMENT, OPENER_EXTRAS, OPENER_TRACK } from './openerBull';
import { t } from '../i18n';

{/* ─────────────────────────────────────────────────────────────────────────
    Charging Bull — assembled longread.
    All three heavy chapters are now NATIVE engine components (no iframes):
    opener candles+3D bull (<ModelChapter> + <CandleIntro>/<StageOverlay>),
    bull map → splat handoff (<MapBullHandoff>), and the S&P charts
    (<ChartsChapter>). The text sections between them are native engine blocks.
   ───────────────────────────────────────────────────────────────────────── */}

{/* ===================== OPENER — Black Monday candles + 3D bull reveal =====
    Native (de-iframed): one chapter section on the timeline. The bull is a
    <ModelChapter> (transparent canvas, track baked from stages.json + explode,
    placed at the authored scale). Two overlay layers ride the SAME scroll:
    <CandleIntro> (candles, first half) and <StageOverlay> (per-stage texts +
    annotations). Camera flights + explode are editable via ?edit; texts/candles
    are styled by hand. */}

<ModelChapter
  src={OPENER_MODEL}
  frames={OPENER_FRAMES}
  loader
  background={[0, 0, 0, 1]}
  vignette
  placement={OPENER_PLACEMENT}
  extras={OPENER_EXTRAS}
  track={OPENER_TRACK}
>
  <CandleIntro span={[0, 0.5]} />
  <StageOverlay
    stagesUrl="/chapters/splash/stages.json"
    range={[0.52, 1]}
    plaques={t('opener.plaques')}
  />
</ModelChapter>

{/* ===================== I · The Way of the Bull → bull scene =============
    De-iframed Mapbox + deck.gl, merged with the Datum bull scene into ONE
    continuous handoff (MapBullHandoff): the map flies through its five stops,
    then on the final dive it zooms into the bull's spot, tilts toward the horizon
    and dissolves to transparent — revealing the bull splat scene that lies
    underneath. The bull then holds (orbitable) and fades to black into II. */}
<MapBullHandoff
  introTitle={t('map.intro.title')}
  introBody={t('map.intro.body')}
  label="bull-env"
  sceneId="32caa065-7eb1-43a1-b8fc-5d6017ed52ad"
  revision="1f515756-64b8-4e85-9dc8-eaa4359cbd1a"
  background={[0, 0, 0, 1]}
  cameraStateOverride={{ position: [97.855, 79.605, 30.468], orbitTarget: [-50.901, 58.09, -3.408], fov: 60 }}
/>

{/* ===================== II · Bulls, Bears & Crises ====================== */}

<BreakReveal
  titleNode={
    <div className="flex items-end justify-center gap-[0.04em]">
      <img src="/chapters/bears-bulls/BEARS.svg" alt={t('bearsBulls.altBears')} className="h-[clamp(46px,9.5vw,128px)] w-auto" />
      <img src="/chapters/bears-bulls/vs.svg" alt={t('bearsBulls.altVs')} className="h-[clamp(24px,5vw,66px)] w-auto mb-[0.28em] mx-[0.1em]" />
      <img src="/chapters/bears-bulls/Bulls.svg" alt={t('bearsBulls.altBulls')} className="h-[clamp(52px,11vw,148px)] w-auto" />
    </div>
  }
  body={t('bearsBulls.body')}
/>

{/* ===================== EXPLAINER — bull / bear market ================== */}

<Prose>
  <div
    className="text-[clamp(17px,1.5vw,22px)] leading-[1.55] text-fg first:[&>b]:font-semibold"
    dangerouslySetInnerHTML={{ __html: t('explainer.lead') }}
  />
  <div dangerouslySetInnerHTML={{ __html: t('explainer.p2') }} />
  <div dangerouslySetInnerHTML={{ __html: t('explainer.p3') }} />
</Prose>

{/* ===================== CHAPTER II — S&P 500 + bear markets =============
    De-iframed: native canvas chart (chartsEngine) driven by the smoothed scroll,
    cards scroll in flow over the pinned chart. */}

<ChartsChapter />

{/* ===================== PROSE — what makes a crisis a crisis =========== */}

<Prose variant="dark">
  <h3 className="font-serif font-semibold text-[clamp(24px,3vw,36px)] text-fg mb-2">
    {t('crisisProse.heading')}
  </h3>
  <div dangerouslySetInnerHTML={{ __html: t('crisisProse.p1') }} />
  <div dangerouslySetInnerHTML={{ __html: t('crisisProse.p2') }} />
  <div dangerouslySetInnerHTML={{ __html: t('crisisProse.p3') }} />
  <div dangerouslySetInnerHTML={{ __html: t('crisisProse.p4') }} />
  <div className="border-l-2 border-gold/50 pl-6 my-10 font-serif italic text-[clamp(18px,1.8vw,26px)] leading-[1.4] text-fg/90">
    {t('crisisProse.quote')}
    <span className="block mt-3 not-italic font-mono uppercase tracking-[2px] text-[11px] text-fg/55">
      {t('crisisProse.quoteAuthor')}
    </span>
  </div>
  <div dangerouslySetInnerHTML={{ __html: t('crisisProse.p5') }} />
</Prose>

{/* ===================== OUTRO ========================================== */}

<Outro
  lines={t('outro.lines')}
  credit={<span dangerouslySetInnerHTML={{ __html: t('outro.credit') }} />}
/>
